export type User = {
  name: string;
  firstName: string;
  lastName: string;
  email: string;
  gender: string;
  title: string;
  dateOfBirth: string;
  phoneNumber: string;
  whatsappNumber: string;
  address: string | null;
  profilePicture: string,
};